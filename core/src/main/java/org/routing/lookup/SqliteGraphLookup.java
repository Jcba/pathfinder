package org.routing.lookup;

import org.routing.geometries.AbstractGeometry;
import org.routing.model.Edge;

public class SqliteGraphLookup implements GraphLookup {

    @Override
    public Edge findClosestEdge(AbstractGeometry<?> geometry) {
        return null;
    }
}
